- hosts: localhost
  become: true
  roles:
    # Shell
    - { role: zsh, tags: "zsh" }
    
    # Programming languages
    - { role: node, tags: "node" }
    - { role: golang, tags: "golang" }

    # Editors
    
    # Dotfiles
    - { role: stow, tags: "stow" }

    # Misc